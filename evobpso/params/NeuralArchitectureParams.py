
class NeuralArchitectureParams:

    def __init__(self, min_layers, max_layers, max_pooling_layers):
        self.min_layers = min_layers
        self.max_layers = max_layers
        self.max_pooling_layers = max_pooling_layers
